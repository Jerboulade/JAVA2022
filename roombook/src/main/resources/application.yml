spring:
  datasource:
    url: jdbc:postgresql://localhost:5435/roombook
    username: postgres
    password: biscotte
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
      format_sql: true