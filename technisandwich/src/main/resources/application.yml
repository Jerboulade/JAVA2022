spring:
  datasource:
    url: jdbc:postgresql://localhost:6969/technisandwich_db
    #url: jdbc:postgresql://localhost:5435/technisandwich_db
    username: postgres
    #password: biscotte
    password: majingohan
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        show_sql: true
        format_sql: true